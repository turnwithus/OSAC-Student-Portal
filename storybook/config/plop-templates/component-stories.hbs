import { {{ pascalCase name }} } from './{{kebabCase name}}';

export default {
  title: '{{ pascalCase type }}s/{{ pascalCase name }}',
  component: {{ pascalCase name }},
};

export const Default = () => {
  return  (
    <div>Default {{ pascalCase name }}</div>
  );
};
